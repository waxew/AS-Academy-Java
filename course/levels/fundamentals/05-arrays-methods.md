# 05 — آرایه‌ها و متدها

- Array یک‌بعدی و چندبعدی
- `Arrays` utility
- Method, Parameter, Argument, Return
- Scope
- Overloading
- Recursion
- Varargs

```java
static int sum(int... values) {
    int result = 0;
    for (int value : values) result += value;
    return result;
}
```

## پروژه
سیستم ثبت نمره با میانگین، کمینه، بیشینه و وضعیت قبولی.
